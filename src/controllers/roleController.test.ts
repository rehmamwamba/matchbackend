// roleController.test.ts

describe('Role Controller', () => {
    it('should create a new role', () => {
        // Test logique pour la création d'un nouveau rôle
    });

    it('should get all roles', () => {
        // Test logique pour la récupération de tous les rôles
    });
});
