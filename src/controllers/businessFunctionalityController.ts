// businessFunctionalityController.ts

import { Request, Response } from 'express';

export async function someBusinessFunctionality(req: Request, res: Response) {
    // Logique pour une fonctionnalité métier spécifique
}
