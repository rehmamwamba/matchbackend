import { Request, Response } from 'express';

export class BusinessFunctionalityController {
    async someBusinessFunctionality(req: Request, res: Response) {
        // Logique pour une fonctionnalité métier spécifique
    }
}
